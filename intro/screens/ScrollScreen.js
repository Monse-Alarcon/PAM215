import { Text, StyleSheet, View } from "react-native"

export default function ScrollScreen() {
    return (
        <View style={styles.container}>
            <Text> Proximamente equipos </Text>
        </View>
    )
}

const styles = StyleSheet.create({})